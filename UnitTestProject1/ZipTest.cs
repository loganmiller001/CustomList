using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using CustomList;
using System.Linq;

namespace UnitTestProject1
{
    [TestClass]
    public class ZipTest
    {
        [TestMethod]
        public void ZipLists_ZipArray_ArraysZipped()
        {
            //MadeUpList<int> aList = new MadeUpList<int>();
            //int[] list1 = new[] {1, 2, 3 };
            //MadeUpList<int> bList = new MadeUpList<int>();
            //int[] list2 = new[] { 4, 5, 6 };
            
            

            //var expected = list1.Zip(list2, (first, second) => first + "" + second);
            //int actual = aList.Zip(aList[], bList[]);
            //Assert.AreEqual(expected, actual);
           
        
        }
    }
}